pipeline {
    agent any

    stages {
        stage('maven build') {
            steps {
                sh '''
                    docker build -f CI\Dockerfile -t maven-build .
                '''
            }
        }
    }
}
